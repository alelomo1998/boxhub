<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
  <!--
    Every suppression added below MUST carry an inline comment explaining WHY it is safe to
    suppress (false positive, not exploitable given how we use the library, accepted risk with
    a tracking reference, etc). An unsuppressed scan that goes noisy is worse than no scan at
    all, because humans start ignoring it -- but a suppression added without a stated reason
    is the same failure mode from the other direction: don't add one just to make the job
    green.

    Empty on purpose: no findings have been triaged yet. Populate only after a real scan
    result surfaces something, never with speculative guesses about what might show up.
  -->
</suppressions>
